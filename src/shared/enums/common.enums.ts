export enum ENUM_GENDER {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum ENUM_PANEL_TYPE {
  USER_APP = 'user-app',
  INTERNAL = 'internal',
  PROVIDER = 'provider',
  EMPLOYER = 'employer',
}

export enum ENUM_BLOOD_GROUP {
  A_POSITIVE = 'a+',
  A_NEGATIVE = 'a-',
  B_POSITIVE = 'b+',
  B_NEGATIVE = 'b-',
  AB_POSITIVE = 'ab+',
  AB_NEGATIVE = 'ab-',
  O_POSITIVE = 'o+',
  O_NEGATIVE = 'o-',
}

export const ENUM_COMMON_STATUS = {
  PENDING: 'pending',
  PROCESSING: 'processing',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
  REJECTED: 'rejected',
};

export enum ENUM_BANNER_POSITION {
  HOME_SCREEN = 'home_screen',
  GREETING_MODAL = 'greeting_modal',
}

export enum ENUM_BANNER_ACTION_TYPE {
  NONE = 'none',
  IN_APP = 'in_app',
  WEBSITE = 'website',
}

export enum ENUM_BANNER_LANGUAGE {
  EN = 'en',
  BN = 'bn',
}
