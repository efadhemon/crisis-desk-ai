export enum ENUM_REPORT_CATEGORY {
  MEDICAL = 'medical',
  FIRE = 'fire',
  ACCIDENT = 'accident',
  CRIME = 'crime',
  FLOOD = 'flood',
  UTILITY = 'utility',
  PUBLIC_SERVICE = 'public_service',
  INFRASTRUCTURE = 'infrastructure',
  OTHER = 'other',
}

export enum ENUM_REPORT_URGENCY {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}

export enum ENUM_REPORT_STATUS {
  PENDING = 'pending',
  IN_REVIEW = 'in_review',
  ASSIGNED = 'assigned',
  RESOLVED = 'resolved',
  REJECTED = 'rejected',
}

export enum ENUM_REPORT_LANGUAGE {
  BN = 'bn',
  EN = 'en',
  UNKNOWN = 'unknown',
}
